# ChronoSheet Backend — Python Dependencies
# To install everything in this list, run:
#   .\venv\Scripts\python.exe -m pip install -r requirements.txt

# Core web framework
fastapi
uvicorn[standard]

# Spreadsheet handling
openpyxl
python-multipart

# Artificial Intelligence + environment variables
groq                      # Groq AI - free, fast Llama 3.3 70B access
python-dotenv             # Loads .env file at startup

# Future additions:
# supabase                # For persistent snapshot storage
# pysyncobj               # For the Raft consensus layer
